import React from "react";
import { PreviewPopupContainer } from "./PreviewPopup.styled";

type Props = {};

const PreviewPopup = (props: Props) => {
  return <PreviewPopupContainer>PreviewPopup</PreviewPopupContainer>;
};

export default PreviewPopup;
